#ifndef MERGE_H
#define MERGE_H
#include <vector>
void MergeS(std::vector<int>& ints, size_t left, size_t mid, size_t right);
void MergeSort(std::vector<int>& ints, size_t left, size_t right);
#endif  // MERGE_H
